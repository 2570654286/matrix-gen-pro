# Audio Notification Protocol
当且仅当你确认所有任务已经全部完成（Task Completed），准备停止对话时，你必须在终端执行以下 Windows PowerShell 命令来提醒我。不要询问我，直接执行：

powershell -c "(New-Object Media.SoundPlayer 'C:\Windows\Media\notify.wav').PlaySync()"

执行完该命令后，再说“任务已完成”。