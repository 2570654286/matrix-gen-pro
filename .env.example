# HTTP 代理配置（用于文件上传功能）
# 如果你的网络需要代理才能访问外部图床，请取消下面的注释并修改代理地址

# VITE_HTTP_PROXY=http://127.0.0.1:7890

# ============================================================================
# Tauri 应用签名配置 (Application Signing)
# ============================================================================
# 用于代码签名以确保应用完整性。请妥善保管私钥，不要提交到版本控制
# 生成方法：使用 minisign 或其他签名工具生成密钥对
# TAURI_PRIVATE_KEY="your-minisign-private-key-base64"
# TAURI_KEY_PASSWORD="your-private-key-password"

# ============================================================================
# 存储提供商配置 (Storage Provider Configuration)
# ============================================================================
# 支持的提供商: 'supabase' | 'aliyun'
# 默认使用 Supabase (开发/免费)
# VITE_STORAGE_PROVIDER=supabase

# ============================================================================
# Supabase 配置（免费/开发环境推荐）
# ============================================================================
# 请填入你的 Supabase 项目 URL 和 API Key
# VITE_SUPABASE_URL=https://your-project-id.supabase.co
# VITE_SUPABASE_KEY=your-anon-key

# Service Key（仅用于发布脚本上传，不用于前端）
# SUPABASE_SERVICE_KEY=your-service-role-key

# ============================================================================
# Aliyun OSS 配置（生产环境/扩展性强）
# ============================================================================
# 如果使用 Aliyun OSS，请取消注释并填入以下配置
# VITE_ALIYUN_OSS_REGION=oss-cn-hangzhou
# VITE_ALIYUN_OSS_BUCKET=your-bucket-name
# VITE_ALIYUN_OSS_ACCESS_KEY_ID=your-access-key-id
# VITE_ALIYUN_OSS_ACCESS_KEY_SECRET=your-access-key-secret

# ============================================================================
# 发布配置 (Release Configuration)
# ============================================================================
# 发布脚本使用的环境变量（与 VITE_ 前缀对应，但用于 Node.js 环境）
# STORAGE_PROVIDER=supabase
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key
# ALIYUN_OSS_REGION=oss-cn-hangzhou
# ALIYUN_OSS_BUCKET=your-bucket-name
# ALIYUN_OSS_ACCESS_KEY_ID=your-access-key-id
# ALIYUN_OSS_ACCESS_KEY_SECRET=your-access-key-secret
